# Server Configuration
NODE_ENV=development
PORT=5001
CLIENT_URL=http://localhost:5173

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/ementech

# JWT Authentication
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Email Configuration (SMTP)
SMTP_HOST=mail.ementech.co.ke
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=admin@ementech.co.ke
SMTP_PASS=Admin2026!

# IMAP Configuration
IMAP_HOST=mail.ementech.co.ke
IMAP_PORT=993
IMAP_USER=admin@ementech.co.ke
IMAP_PASS=Admin2026!

# Email Server Configuration
EMAIL_DOMAIN=ementech.co.ke
EMAIL_FROM=noreply@ementech.co.ke
EMAIL_FROM_NAME=EmenTech

# ============================================================================
# EMAIL SECURITY (DKIM/SPF/DMARC)
# ============================================================================
# DKIM signing key (generate with: node scripts/email-diagnostics.js --generate-dkim)
# DKIM_SELECTOR=default
# DKIM_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# ============================================================================
# MAILBOX CREATION METHOD
# ============================================================================
# Options: manual, ssh, database, api
MAILBOX_CREATION_METHOD=manual

# For SSH method:
# MAIL_ADMIN_HOST=mail.ementech.co.ke
# MAIL_ADMIN_USER=root
# MAIL_ADMIN_SSH_KEY=/path/to/private/key

# For Database method (virtual users):
# MAIL_DB_HOST=localhost
# MAIL_DB_USER=mailadmin
# MAIL_DB_PASSWORD=your_db_password
# MAIL_DB_NAME=mailserver

# For API method:
# MAIL_ADMIN_API_URL=https://mail.ementech.co.ke/api
# MAIL_ADMIN_API_KEY=your_api_key

# ============================================================================
# REDIS (for email queue)
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# CORS
CORS_ORIGIN=http://localhost:5173
